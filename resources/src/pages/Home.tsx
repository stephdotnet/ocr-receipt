import {Container, Typography} from "@mui/material";

const Home = () => {
  return <Container>
    <Typography variant="h1" textAlign="center">
      Home
    </Typography>
  </Container>;
}

export default Home;